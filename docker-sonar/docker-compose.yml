version: '3.8'

services:
  sonarqube:
    image: sonarqube:9.9-community
    container_name: sonarqube_cnes_local
    ports:
      - "9000:9000"
    volumes:
      - ./plugins:/opt/sonarqube/extensions/plugins
    environment:
      - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true